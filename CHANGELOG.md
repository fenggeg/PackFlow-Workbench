# 更新日志

本文件用于记录每个正式版本的更新内容。GitHub Actions 发布 `v*` 标签时，会读取对应版本小节并写入 GitHub Release 和 `latest.json.notes`，应用内"检查更新"弹窗也会展示这里的内容。

## [3.3.12] - 2026-09-23

### 新增

- **JAR 内容查看**：产物列表可直接打开 jar / war / ear / zip，以目录树方式浏览归档结构（目录可展开折叠、支持一键展开全部），也可按路径搜索，命中时自动展开所在层级。文本类文件（properties / xml / yml / json / txt 等）可直接查看内容并复制，二进制条目（如 class）会明确提示不支持预览；同时可查看 MANIFEST。确认配置是否打进去、参数写得对不对，不必再解压或另开 IDE。
- **JAR 条目修改**：文本类条目（`application.yml`、`logback.xml`、`bootstrap.properties` 等）可在应用内直接编辑并写回归档，省去解压再压缩；未修改的条目按原始压缩数据搬运，保存速度接近文件复制；保存前自动把原文件备份到同目录（`.bak-时间戳`），若归档带数字签名则同步移除签名文件（否则修改后 Java 会拒绝加载），保存过程有明确进度提示且不允许中途关闭，并在保存前说明产物将与源码不再一致。

## [3.3.11] - 2026-09-22

### 新增

- **JAR 条目修改**：文本类条目（`application.yml`、`logback.xml`、`bootstrap.properties` 等）可在应用内直接编辑并写回归档，省去解压再压缩；未修改的条目按原始压缩数据搬运，保存速度接近文件复制；保存前自动把原文件备份到同目录（`.bak-时间戳`），若归档带数字签名则同步移除签名文件（否则修改后 Java 会拒绝加载），保存过程有明确进度提示且不允许中途关闭，并在保存前说明产物将与源码不再一致。
- **JAR 内容查看**：产物列表可直接打开 jar / war / ear / zip，以目录树方式浏览归档结构（目录可展开折叠、支持一键展开全部），也可按路径搜索，命中时自动展开所在层级。文本类文件（properties / xml / yml / json / txt 等）可直接查看内容并复制，二进制条目（如 class）会明确提示不支持预览；同时可查看 MANIFEST。确认配置是否打进去、参数写得对不对，不必再解压或另开 IDE。

## [3.3.10] - 2026-09-22

### 优化

- **模块勾选联动**：逐个勾选同一父模块下的所有子模块后，父模块自动进入勾选状态，父子选择保持一致；只勾选部分子模块时父模块保持未勾选。

## [3.3.9] - 2026-09-22

### 修复

- **模块勾选残留**：父模块勾选后逐个取消其子模块，父模块不再残留勾选状态，避免出现「父模块选中但子模块全空」的无效选择。
- **快捷键提示**：命令面板快捷键提示改为仅首次启动提示一次，不再每次打开软件都弹出。

## [3.3.8] - 2026-09-22

### 新增

- **构建队列**：支持把当前配置加入队列，或多选已保存项目批量排队；当前构建结束后自动继续下一项，每项状态与耗时可见，等待项可移出队列。
- **构建前检查**：构建开始前自动检查项目目录、模块路径、JDK（含 javac 校验）、Maven / Wrapper、settings.xml 与本地仓库，未通过项直接拦住并说明原因，也可在构建页手动运行一次确认。
- **历史日志回看**：构建日志路径随记录持久化，历史列表可随时打开当次构建的完整日志，不必再去应用数据目录里手工翻文件。
- **模板导入**：构建模板支持从 JSON 导入，补齐导出闭环；导入项重新生成标识并加「（导入）」后缀，只新增不覆盖已有模板。
- **诊断定位错误**：诊断结果新增「定位错误」按钮与可点击的关键日志列表，一键切换到日志页签并高亮首个错误行，定位时自动暂停日志跟随。
- **依赖结构树**：按层级展开当前模块的传递依赖，循环依赖单独标记，节点可点击逐层跳转查看。
- **构建耗时基线**：基于同项目同模块范围的历史成功构建给出平均耗时，并在构建中提示预计剩余时间。
- **产物差异对比**：构建成功后与上一次同范围构建对比，展示新增、体积变化与本次未产出的产物，便于快速发现异常。
- **构建范围预览**：启用 `-am` 时，模块树提前提示连同上游依赖共有多少个模块参与构建。
- **历史趋势统计**：历史页新增成功率、平均耗时、近 7 天构建次数与最慢模块。
- **命令面板入口**：快捷键本身不可见，顶栏新增常驻入口并直接展示快捷键组合，启动后轻提示几次（上限 2 次），面板底部同步标注操作键位。

## [3.3.7] - 2026-09-22

### 新增

- **数据备份与恢复**：顶栏新增「数据与诊断」入口，可备份本地数据库到指定位置、从备份恢复（校验文件头，避免误导入），一键打开应用数据目录。
- **诊断包导出**：一键导出包含版本、项目、环境、构建参数、命令、诊断结果与最近日志的诊断文件，排障不再需要手工翻应用数据目录。
- **命令面板**：`Ctrl + K`（macOS 为 `Cmd + K`）打开命令面板，可跳转页面、开始/停止构建、刷新环境、扫描 JDK、切换主题，并直接切换到已保存项目。
- **数据导出**：历史记录支持导出当前筛选结果为 CSV（带 BOM，Excel 打开不乱码），构建模板与依赖冲突结果支持导出 JSON。
- **日志正则搜索**：日志搜索支持正则模式，命中内容高亮显示；正则非法时给出提示并暂停过滤。
- **Git 信息**：展示本地领先远端的提交数量；提交列表支持按说明 / 作者 / hash 搜索并分页浏览。
- **依赖可视化**：依赖洞察展示循环依赖的具体路径，模块标签可点击跳转到对应模块。

### 修复

- **手工编辑命令失效**：修复「完整命令预览」中手工保存的命令在构建时被自动生成结果覆盖的问题；手工保存后命令进入锁定态，参数改动不再覆盖，可随时「恢复自动生成」解除。
- **设置损坏被静默清空**：本地设置解析失败时不再用默认值覆写，改为先隔离备份原始内容再报错，避免 JDK 注册表与环境方案永久丢失。
- **并发写入失败**：数据库开启 `busy_timeout`，历史写入与「仅保留 100 条」清理合并为一个事务，修复并发场景下的 `SQLITE_BUSY`。
- **坏记录拖垮整表**：历史与模板改为逐行容错，单条损坏记录只影响自身并记日志，不再让整个列表不可用。
- **切换项目后冲突结果残留**：切换项目时清空依赖冲突扫描结果，不再展示上一个项目的结论。
- **模板跨项目误用**：应用属于其它项目的构建模板时明确拒绝并提示，避免把构建根目录切到别处。
- **诊断依据不完整**：诊断前先冲刷缓冲日志，最后一批日志也参与失败原因分析。
- **删除产物缺少约束**：物理删除必须有项目根目录做路径约束，缺失时直接拒绝。
- **JRE 被当作 JDK**：JDK 判定增加 `javac` 校验，扫描与手动登记都会过滤掉 JRE，避免「环境正常」但构建必然失败。
- **取消构建误报失败**：进程刚启动尚未登记时取消，现在会正确标记为已取消而不是失败。
- **重复启动构建**：构建进行中再次点击开始会被拦截，避免产生无人跟踪的孤儿进程。

### 优化

- **构建失败诊断**：规则改为按权重选最优原因，不再取「数组中第一个命中」；新增编译错误、内存不足、插件解析失败、JRE 缺少编译器、编码错误等高频类别；修正常见日志文本造成的误判。
- **环境检测**：外部命令（`where` / `-version`）增加 10 秒超时并缓存结果，「刷新环境」会强制取最新值，避免卡死或反复跑子进程。
- **项目绑定**：绑定键统一规范化（分隔符方向与大小写），绑定与解绑更可靠；切换项目不再把上一个项目的环境方案提升为全局方案。
- **默认 JDK**：注册表支持「设为默认」，重新扫描系统 JDK 后保留原默认项。
- **进度展示**：日志中长时间没有可识别阶段信号时（如手工命令、`-q` 模式）降级为「进度无法确定」，不再展示编造的百分比。
- **依赖冲突扫描**：增加 10 分钟超时上限与真正的取消（终止 `dependency:tree` 进程）；扫描异常退出时明确提示结果可能不完整，不再被误读为「没有冲突」。
- **产物扫描**：默认只展示本次构建写过的产物（构建未重新打包时自动回退），并支持 zip / ear / tar.gz 等分发包类型。

## [3.3.6] - 2026-09-21

### 新增

- **节假日倒计时**：首页新增节假日倒计时卡片，展示今天及未来最近的节假日与剩余天数，并提醒调休上班安排。

### 优化

- **首页布局**：移除「正在运行任务」卡片，将「构建产物」「构建历史」快捷操作移入页头，与「开始打包」并列，操作更集中。

### 修复

- **应用更新**：修复软件启动时静默检查更新不生效的问题，现在启动后会自动检查新版本。

## [3.3.5] - 2026-09-21

### 优化

- **构建侧边栏**：进入构建页时默认展示模块列表，方便直接选择要打包的模块。
- **应用更新**：更新检查对话框关闭后保留新版本提示，顶栏「检查更新」按钮显示红点提醒；发现新版本时自动重新打开更新弹窗。

## [3.3.4] - 2026-09-21

### 优化

- **应用更新**：应用内更新重构为官方更新插件，下载、签名校验、安装与自动重启一步完成，更新流程更稳定可靠。
- **更新源**：新增自建更新源作为首选，GitHub 作为兜底，更新检查与下载更快速、更稳定。
- **官网下载**：官网下载入口改为读取最新发布版本信息，自动展示版本号与安装包文件名。

## [3.3.3] - 2026-09-21

### 修复

- **更新日志**：修复更新弹窗内更新日志排版错乱的问题，标题、列表、代码块等现在能正确渲染。
- **应用更新**：修复安装更新后版本未生效、需多次重试才更新的问题，安装前会确保旧进程完全退出，一次安装即可生效。

## [3.3.2] - 2026-09-21

### 新增

- **首页信息**：首页新增系统时间与网络状态卡片，实时展示当前时间、公网 IP、归属地与运营商，支持手动刷新与自动更新。
- **产物列表**：产物列表改为表格展示，文件名、类型、大小、模块与修改时间一目了然，支持分页浏览。

### 优化

- **项目切换**：项目切换改为顶栏入口弹出对话框，已保存项目支持模糊搜索快速定位，对话框排版更紧凑协调。
- **界面动效**：页面切换、侧边栏页签、构建进度与历史记录等增加过渡动画，交互反馈更明显。
- **历史统计**：历史页顶部新增构建统计卡片，总计 / 成功 / 失败一目了然。

## [3.3.1] - 2026-09-21

### 新增

- **外部链接入口**：顶栏新增 GitHub 发布页与官网图标，点击即可在浏览器中打开对应页面。

## [3.3.0] - 2026-09-21

### 新增

- **打包进度可视化**：构建时实时展示进度百分比、当前阶段（准备 / 构建模块 / 扫描产物 / 完成）与子步骤（清理 / 编译 / 测试 / 打包 / 安装）；模块数量与序号按构建日志中的 Reactor 模块清单统计。底部操作栏同步显示细进度条，失败时给出明确提示并可一键跳转诊断，完成后进度保留，可手动收起或在下一轮构建时重置。
- **深色主题**：新增外观切换，支持浅色、深色与跟随系统。
- **日志复制**：日志支持鼠标框选，选中后可一键复制选中内容，也可复制全部日志或当前筛选结果。

### 优化

- **界面体系重构**：统一设计令牌与组件库，界面风格、间距、圆角与配色更一致，浅色与深色主题观感统一。
- **启动画面**：启动窗口的标识、配色与进度条改为与产品内一致的视觉，并跟随系统深浅色。
- **错误反馈**：构建、环境、产物、模板等操作的失败不再静默，统一以通知形式提示原因。
- **模块列表**：点击整行即可切换是否加入构建，模块名更醒目，依赖洞察默认展开。
- **历史记录**：支持按模块或命令搜索、按构建结果筛选，单页显示更多记录。
- **日志阅读**：可关闭自动换行以保留原始列对齐；滚动条样式与整体风格统一；向上翻阅时自动暂停自动滚动；日志中的颜色控制符不再造成换行错位。
- **检查器与环境配置**：检查器入口移至顶栏右上角，仅在需要时出现；环境路径改为显式保存，不再因关闭弹窗丢失输入。

### 修复

- **应用内更新**：修复更新包下载地址被发布源白名单误拦、导致"能检测到更新却无法下载"的问题；下载地址校验改为逐个候选容错，单一地址变更不再阻断更新，失败原因也会如实展示而不再被泛化提示掩盖。
- **构建命令参数**：修复勾选顺序不同可能生成错误构建目标顺序的问题；常用开关、并行线程数与自定义参数不再相互覆盖。
- **构建范围**：只选择一个子模块时，不再连带把父聚合模块加入构建范围，构建命令与历史记录保持一致。
- **进度统计**：修复模块总数被 jar 打包输出放大（出现 23/24 之类错误）的问题，模块序号改为按日志中的 Reactor 清单精确统计。
- **阶段进度**：兼容 Maven 短名插件日志，清理 / 编译 / 测试 / 打包等子步骤不再停滞不动。
- **依赖冲突扫描**：切换侧边栏页签不再丢失扫描进度与结果，取消按钮如实提示为"放弃等待"。
- **Git 操作**：拉取或切换分支后不再清空已选模块、构建日志与诊断结果。
- **日志选中**：修复深色日志区域选中文本看不清的问题。
- **导航栏设置**：修复"恢复默认"不生效的问题。
- **其他**：重跑历史记录前增加二次确认；修复重跑、放大查看、切换页签后状态丢失等若干问题。

## [3.2.3] - 2026-09-11

### 修复

- **界面样式**：修复 v3.2.2 因内容安全策略（CSP）过严导致全局样式失效的问题，已恢复为与 v3.2.1 一致的加载行为。

## [3.2.2] - 2026-09-11

### 修复

- **构建状态卡住**：修复构建极快完成时界面可能一直显示"构建中"的问题，构建结束状态与历史记录现在能可靠落库。
- **构建日志串台**：修复上一次构建结束后的零散日志可能混入下一次构建的问题；大量日志时界面也更流畅。
- **构建中切换项目**：构建进行中切换或拉取项目会被拦截，避免后台进程变成无人跟踪的孤儿任务。
- **取消构建**：取消与等待退出的流程更可靠，避免重复结束事件或句柄误用。
- **更新安装安全**：限制更新包只能从 GitHub 发布源下载，并校验安装包文件名，防止路径异常写入。
- **依赖冲突扫描**：修复扫描过程中因输出缓冲堵塞导致长时间无响应的问题。
- **删除构建产物**：删除操作限制在当前项目目录内，避免误删项目外文件。
- **命令转义**：构建命令预览中路径与参数在含空格或特殊字符时正确加引号，降低命令被错误拆分的概率。

### 优化

- **启动与退出**：应用关闭时后台清理构建进程，不再阻塞退出；构建并发上限校验更严格。
- **官网下载**：官网版本检查改为同源接口，规避浏览器跨域限制。

## [3.2.1] - 2026-09-07

### 修复

- **产物复制**：修复复制构建产物到剪贴板后，粘贴到 SSH 客户端等仅识别文本格式的程序时中文文件名出现乱码的问题，现在同时写入文件引用与路径文本两种格式，粘贴到资源管理器和 SSH 客户端均能正常显示。
- **版本检查**：修复官网最新版本接口跨域调用失败的问题，并移除预览区多余图标。

## [3.2.0] - 2026-08-31

### 优化

- **应用精简**：移除命令中心、服务器管理、服务运维、部署等非核心模块，聚焦构建核心功能，简化界面与操作流程，提升启动速度与运行稳定性。

## [3.1.0] - 2026-07-10

### 新增

- **依赖冲突检测**：新增依赖冲突检测面板，通过 `mvn dependency:tree -Dverbose` 分析项目各模块的传递依赖冲突，展示冲突版本与依赖路径。
- **排除代码生成**：针对冲突依赖支持生成对应的 Maven `<exclusions>` 排除代码，可复制到剪贴板直接粘贴到 POM 中使用。
- **批量排除**：支持按模块一键批量生成该模块下所有冲突依赖的排除代码。

### 修复

- **冲突面板布局**：修复模块名称过长时与"批量排除"按钮重叠的问题，现模块名独立成行完整展示，不再被截断。

## [3.0.11] - 2026-07-06

### 新增

- **并发构建限制**：支持配置最大并发构建数，避免同时启动过多构建任务占用系统资源，可在高级参数面板中设置。
- **应用退出清理**：关闭应用时自动终止所有运行中的构建进程，避免残留后台进程。

### 优化

- **SSH 连接复用**：SSH 连接池缓存 SFTP 和命令会话，同一服务器重复操作时复用已有连接，减少重复握手开销。
- **数据库并发性能**：SQLite 切换为 WAL 模式，支持多连接并发读写，提升数据操作响应速度。
- **POM 解析缓存**：POM 文件解析结果基于修改时间缓存，依赖分析等场景避免重复解析同一文件。
- **Base64 上传内存**：Base64 回退上传方式改为流式分块处理，不再将整个文件读入内存，大幅降低大文件上传时的内存占用。

## [3.0.10] - 2026-07-03

### 新增

- **模板导出导入**：命令模板管理支持导出和导入，可备份模板到文件或从文件恢复，导出时可选择仅导出当前选中的模板或导出全部。

## [3.0.9] - 2026-07-02

### 修复

- **上传步骤路径模式**：修复编辑模板时上传步骤选择"使用变量"后保存，重新打开会变成"固定路径"的问题，路径模式现在会被正确持久化。

## [3.0.8] - 2026-07-01

### 修复

- **断开日志按钮**：修复勾选"不影响状态"的后台命令（如 tail -f）执行完成后断开日志按钮不显示的问题。
- **断开日志无响应**：修复点击断开日志时程序卡死无响应的问题，改为非阻塞取消机制避免死锁。
- **断开日志重复提示**：修复断开日志时打印两条"日志连接已断开"的问题。

## [3.0.7] - 2026-06-29

### 新增

- **步骤插入**：部署模板编辑器支持在任意步骤前插入新步骤，不再只能追加到末尾。
- **日志断开**：实时日志查看支持手动断开连接，tail -f 日志可随时停止。
- **变量来源**：部署模板变量支持配置产物来源，灵活指定变量的取值方式。

### 优化

- **SFTP 连接池**：服务器文件管理复用 SSH 连接，避免切换页面时重复建立连接，操作更流畅。
- **远程目录创建**：上传文件时确保远程目录存在，使用幂等操作替代先检查再创建。

### 修复

- **上传路径空格**：修复远程路径含前导空格时 SFTP 创建空名称目录导致 jar 包无法访问的问题。
- **SFTP 文件写入**：上传文件后显式关闭远程文件句柄，确保服务端数据完整落盘。

## [3.0.6] - 2026-06-29

### 优化

- **启动动画**：改用原生启动窗口替代前端 CSS 动画，彻底消除窗口打开瞬间的白屏，展示品牌图标与加载动画。

## [3.0.5] - 2026-06-26

### 新增

- **远程文件上传**：服务器文件管理支持上传本地文件到远程服务器，显示实时传输进度。
- **远程文件下载**：服务器文件管理支持下载远程文件到本地，弹出保存位置选择并显示进度。
- **启动动画**：应用启动时显示品牌化加载画面，消除白屏等待。

### 优化

- **模板编辑器**：从弹窗改为抽屉式编辑器，表单垂直布局更宽敞，步骤和变量编辑体验优化。
- **变量编辑器**：从内联表单改为卡片式垂直布局，输入框更宽，路径变量交互更清晰。
- **模板列表**：选中状态改用绿色主题，修复选中时布局跳动问题。
- **路径变量默认值**：本地路径默认文件选择器模式，远程路径默认手动输入。
- **构建产物过滤**：下拉列表只显示实际存在的文件，按最新排序。

### 修复

- **下拉选项换行**：修复模板变量下拉选项在光标后无法回车换行的问题。

## [3.0.4] - 2026-06-24

### 新增

- **变量下拉选择**：命令模板变量支持下拉选择类型（type="select"），可配置选项列表，变量编辑器自动渲染为 Select 组件。

### 优化

- **检查器浮动模式**：检查器（Inspector）从固定右侧抽屉改为浮动面板，主工作区获得更多空间，可通过右上角按钮快速切换。
- **模板编辑器增强**：模板编辑器支持为变量配置类型（文本/下拉）和下拉选项，配置更灵活。

## [3.0.3] - 2026-06-23

### 修复

- **模板变量输入**：修复模板编辑器中变量标识输入框输入一个字符后立即失焦的问题，改用稳定的索引作为列表 key。

## [3.0.2] - 2026-06-23

### 修复

- **首页路由**：修复首页"服务器管理"按钮点击后跳转到服务器详情页的问题，改为跳转到服务器列表页面。

## [3.0.1] - 2026-06-23

### 新增

- **步骤超时配置**：支持为命令步骤设置超时时间（0-3600秒），超时后自动终止。
- **不影响状态选项**：步骤配置新增"影响状态"开关，关闭后该步骤失败不影响整体执行状态。
- **断开连接按钮**：执行日志面板右上角添加"停止"按钮，可主动中断正在运行的命令。
- **分页显示**：执行历史改为每页10条记录，添加分页控件。
- **统一滚动条样式**：为日志面板添加自定义滚动条，与其他组件保持一致。

### 优化

- **后台运行**：配置了"不影响状态"的步骤在后台线程运行，不阻塞整体状态判断。
- **Git 忽略配置**：添加 .gitignore 文件，移除 .fastRequest 和 .qoder 目录的跟踪。

### 修复

- **错误提示**：本地文件不存在时，前端日志会显示 ❌ 错误信息。
- **SFTP 调试**：添加 SFTP 会话创建的调试日志，便于排查上传问题。

## [3.0.0] - 2026-06-23

### 新增

- **命令调度中心**：全新的命令驱动调度平台，替代原有复杂部署系统。
  - 命令模板系统：支持创建可复用的命令模板，每个步骤为 shell 命令或文件上传。
  - 模板管理面板：独立的模板列表视图，支持新建、编辑、复制、删除操作。
  - 命令执行器：支持按顺序执行模板步骤，实时日志输出，支持取消执行。
  - 变量替换系统：支持 `{{variable}}` 语法，内置常用变量（artifactPath、serverHost、timestamp 等）。
  - 等待步骤类型：支持在命令链中添加等待时间，可配置等待秒数。
  - 文件选择器：上传步骤支持通过系统文件管理器选择本地文件。
  - 执行历史：记录所有命令执行记录，支持查看日志和删除。
- **服务器搜索**：命令调度中心的服务器选择支持搜索过滤。
- **构建产物快捷传递**：构建成功后可直接将产物路径发送到命令调度中心。

### 优化

- **模板管理独立化**：模板管理与执行控制分离，左侧显示模板列表，右侧显示执行配置。
- **远程路径自动校验**：自动将反斜杠转为正斜杠，提示 Linux 路径格式。
- **上传进度防抖动**：使用等宽字体、固定宽度容器和节流更新（100ms）避免 UI 抖动。
- **日志样式统一**：命令执行日志使用项目统一的 `.log-panel` 和 `.log-line` CSS 类。
- **导航配置迁移**：新增 persist 迁移逻辑，自动清除已删除页面的缓存配置。

### 移除

- **发布向导服务**：删除 ReleasePage、useReleaseStore 及相关引用。
- **服务运维页面**：删除 ServicePage，导航中移除"服务"入口。
- **旧部署系统**：删除 deployment_executor、startup_probe_service、deployment_common 等后端服务。
- **旧前端组件**：删除 Deployment 目录、deploymentRuntime、deploymentTopologyService 等。
- **旧状态管理**：删除 useDeploymentLogStore、useDeploymentTemplateStore、useUploadProgressStore。

### 说明

- 本次为大版本升级，部署系统完全重构为命令调度平台。
- 远程路径必须使用 Linux 格式（正斜杠 `/`），例如 `/home/data/app.jar`。
- 命令调度中心支持三种步骤类型：命令（command）、上传（upload）、等待（wait）。

## [2.0.13] - 2026-06-19

### 新增

- JDK 注册中心：支持从 pom.xml 自动解析项目 JDK 版本需求，扫描系统中已安装的 JDK（覆盖 Program Files、Scoop、IntelliJ 等常见路径），统一管理注册表。
- 环境配置按项目独立记忆：每个项目的 JDK、Maven、settings.xml、本地仓库、执行器等配置互不干扰，首次手动修改后自动绑定并记忆，未修改过的配置保持自动识别。

### 优化

- 环境中心 UI 简化：移除方案选择器下拉框和绑定状态栏，JDK 行改为点击弹出切换器，选择后自动保存并刷新环境。

## [2.0.12] - 2026-06-18

### 修复

- 修复 NSIS 安装程序公司名称显示为 "local" 的问题，现在通过显式配置 `publisher` 字段正确显示为 "PackFlow"。

## [2.0.11] - 2026-06-16

### 新增

- 构建历史记录支持删除操作，在历史记录列表中可直接删除单条构建记录。
- 服务操作历史支持删除操作，可清理不再需要的服务操作记录。
- 产物管理删除时新增"仅删除记录，不删除文件"选项，通过复选框控制是否保留物理文件。

### 优化

- 部署中心"发布映射"和"部署模板"的新增按钮移至 Tabs 标签栏右侧，与标签对齐显示，操作更便捷。
- SSH 连接建立时，当 ssh2 辅助连接失败不再静默忽略，而是自动回退到主会话执行命令，提高连接兼容性。

### 修复

- 修复构建中心产物复制到剪贴板时内存分配计算错误（多分配 2 字节）的问题。
- 修复构建中心产物复制时 `SetClipboardData` 返回值检查逻辑错误，现在通过 `GetLastError` 判断是否真正出错。
- 修复服务重启时因 ssh2 连接失败导致报错"当前 SSH 连接不支持实时日志会话"的问题，现在会自动回退使用主 SSH 会话执行命令。

## [2.0.10] - 2026-05-27

### 优化

- 检查更新改为由 Rust 后端请求自定义 GitHub Release 转发接口，并启用系统代理支持，代理环境下检查和下载链路表现更一致。
- 更新包下载改为用户点击后才开始，不再在检测到新版本时自动后台下载。
- 下载和安装拆分为两个手动步骤，下载完成后需再次确认安装，避免误触发应用退出和安装流程。
- 更新安装包缓存到本机用户目录，下次检查更新时若缓存包完整可直接提示安装，无需重复下载。
- 更新弹窗新增下载速度展示，并在下载完成后保留"等待安装"状态提示。

### 修复

- 修复更新安装后应用未自动重新打开的问题，现在会在当前应用退出后执行静默安装，并在安装成功后自动启动新版本应用。
- 修复通过 GitHub 下载入口返回非安装包内容时缺少校验的问题，现在会校验响应类型和安装包大小，避免缓存损坏文件。

## [2.0.9] - 2026-05-27

### 测试

- 测试检查更新

## [2.0.8] - 2026-05-27

### 测试

- 测试检查更新

## [2.0.7] - 2026-05-27

### 修复

- 修复检查更新接口被 CDN 或浏览器缓存导致最新版本应用检测到旧版本更新的问题，请求现在携带时间戳参数和缓存控制头，确保每次检查都获取最新版本信息。

## [2.0.6] - 2026-05-27

### 修复

- 修复检查更新接口返回完整 GitHub Release 对象时，应用无法正确识别安装包导致提示"检查更新失败"的问题。
- 修复安装包资源筛选条件过窄的问题，现在会优先选择 `PackFlow.Workbench_x64-setup.exe`，并兼容带版本号的 `x64-setup.exe` 安装包。

## [2.0.5] - 2026-05-27

### 优化

- 远程终端输出链路改为后端事件推送，前端监听 `terminal-output:{sessionId}` 并仅使用 xterm.js `term.write` 渲染远程输出，降低轮询开销。
- 远程终端创建时使用交互式 SSH Shell PTY，并按 xterm.js 当前列数和行数初始化终端尺寸。
- 远程终端支持 resize 事件透传，前端窗口尺寸变化后会将最新列数和行数同步到后端终端会话线程。
- 增加远程终端调试日志，记录前端输入字节长度、后端 `terminal_write` 接收长度以及 SSH shell 写入成功或失败信息。

### 修复

- 修复远程终端连接成功但无法输入命令的问题，用户输入现在通过 xterm.js `onData` 原样发送到 Tauri 后端，不再用 `term.write` 模拟输入。
- 修复 SSH shell 读循环阻塞导致写入永远无法执行的问题，终端会话现在通过独立 `input_tx` 队列把输入交给会话线程统一写入远程 PTY，避免 read/write 竞争同一个 shell 对象。
- 修复 Enter、Backspace、Tab、方向键、Ctrl+C、Ctrl+D、Ctrl+L 等特殊按键无法可靠透传的问题。
- 修复远程终端空闲时后台持续打印 `ssh::model::timeout` 超时日志的问题，保留短超时读取以保障输入响应，同时过滤第三方库的轮询噪声。

## [2.0.4] - 2026-05-27

### 优化

- 改造应用内检查更新逻辑，移除对 GitHub Releases `latest.json` 的直接依赖，改为调用自定义更新接口获取版本信息。
- 更新检查支持 GitHub Release API 响应格式，自动解析版本号、更新日志和安装包下载链接。
- 下载更新包改用浏览器原生 `fetch` 流式下载，支持实时进度回调。
- 安装更新改为 Rust 后端直接执行 NSIS 静默安装，无需 Tauri updater 插件参与。

### 新增

- 新增自定义版本比较算法，支持语义化版本号（SemVer）精确比较，跳过草稿和预发布版本。
- 新增 Rust 后端 `install_app_update` 命令，支持将安装包写入临时目录并执行静默安装。

## [2.0.3] - 2026-05-26

### 新增

- 新增"服务器"独立页面，从部署中心拆分出服务器管理，包含服务器列表、详情查看和编辑功能，部署中心专注于发布映射配置与部署执行。
- 新增服务器详情页，包含概览、远程终端、远程文件、远程日志、常用命令 5 个 Tab，支持一站式服务器运维操作。
- 新增远程终端功能，基于 xterm.js 提供 SSH 交互式终端，支持密码和私钥认证，可直接在应用内操作远程服务器。
- 新增远程文件浏览，支持查看服务器目录结构和文件内容。
- 新增远程日志查看，支持实时 tail 远程日志文件，可配置日志源路径。
- 新增常用命令管理，支持保存和快速执行服务器常用命令。
- 新增一键部署功能，构建成功后可直接选择服务器发起部署，实现"构建→部署"无缝衔接。
- 新增前端静态资源发布支持，部署执行器新增前端静态资源部署类型，支持目录压缩、文件上传和发布流程；"服务映射"统一更名为"发布映射"以涵盖前端和后端两种部署场景。
- 新增"发布"页面（Release），集中管理发布映射配置、模板和发布操作。
- 新增导航栏设置功能，支持自定义导航栏显示顺序和可见性，支持指定启动时默认打开的页面，配置持久化保存。
- 新增导航栏设置入口，底部齿轮图标一键打开设置弹窗。
- 新增日志面板一键复制按钮。

### 优化

- SSH 连接算法扩展，ssh-rs 新增 DiffieHellmanGroup14Sha256 和 DiffieHellmanGroup14Sha1 密钥交换算法，ssh2 新增 Aes192Ctr 加密算法，提升与不同 SSH 服务器的兼容性。
- 远程终端改用 ssh-rs 纯 Rust SSH 库替代 ssh2（libssh2），解决 Windows 上因 WinCNG 后端不支持现代加密算法导致的握手失败问题，无需额外安装 OpenSSL。
- 服务器列表增加连接状态和权限信息列，展示更丰富的服务器信息。
- 服务运维页面仅显示后端服务类型，排除前端静态资源。
- 更新应用 Logo 和 Favicon 为 PNG 格式。

### 修复

- 修复日志输出放大查看时未自动滚动到底部的问题，BuildLogPanel、DeploymentHistoryTable、RemoteLogViewer 和 ServicePage 均已修复。
- 修复 SSH 连接握手失败（Unable to exchange encryption keys）的问题，终端和 SFTP/命令通道均使用统一的算法协商策略。
- 修复服务器认证方式变更后终端无法重新连接的问题。

## [2.0.2] - 2026-04-30

### 新增

- 新增服务器提权（Privilege）配置，支持 sudo、sudo -i、su 和自定义命令包装四种提权模式，可指定运行用户、提权密码来源和 Shell 类型。
- 新增提权密码安全存储，独立提权密码使用 Windows DPAPI 加密持久化，登录密码提权模式复用服务器登录凭据。
- 新增部署暂存目录机制，提权模式下产物先上传到用户可写临时目录（默认 `~/.packflow/deploy/{deploymentId}`），再由提权用户移动到目标路径，解决普通账号无目标目录写权限的问题。
- 新增部署成功后自动清理暂存目录，支持配置失败时保留暂存目录用于排查。
- 新增服务映射项目归属，服务映射配置绑定项目根路径，服务页面仅展示当前项目关联的服务，跨项目切换时不再混淆。
- 新增服务映射模块路径与 artifactId 冗余存储，模块 ID 匹配失败时按路径回退查找，项目重新解析后服务映射不丢失。
- 新增 LogConsole 通用日志控制台组件，统一构建日志和部署日志的渲染逻辑，支持日志行数上限折叠和空状态提示。
- 新增 UI 设计令牌体系（uiTokens），颜色、圆角、间距、阴影、字号统一收敛为 CSS 变量，为后续主题切换奠定基础。
- 新增部署流水线进度摘要计算服务（deploymentRuntime），统一部署历史和服务页面的进度条与失败阶段定位逻辑。
- 新增上传进度节流策略，按百分比增量和时间间隔控制进度事件刷新频率，减少高频事件带来的渲染开销。
- 新增 boundedBuffer 工具函数，为日志和进度等增长型数据提供容量上限裁剪。

### 优化

- 应用更名为"PackFlow Workbench（打包部署工作台）"，产品标识、窗口标题和更新端点统一更新。
- 部署执行器所有远程命令统一走提权通道，启动探针（进程、端口、HTTP、日志）和备份查找均使用提权执行，确保非 root 部署场景下探针和回滚正常工作。
- 替换类步骤自动补全 `mkdir -p`，避免目标目录不存在时 `mv` 命令失败。
- SFTP 上传目录创建路径使用 shell_quote 转义，修复含空格路径上传失败的问题。
- SSH 传输层新增独立 SSH2 会话用于提权命令执行，支持 stdin 传入提权密码，避免与主会话和 SFTP 会话冲突。
- 环境中心方案管理改为弹窗交互，新建和编辑方案名称不再占用侧栏空间，操作更聚焦。
- 服务页面按当前项目过滤服务映射，新增服务器搜索和日志全屏查看，部署日志使用 LogConsole 统一渲染。
- 部署历史日志使用 LogConsole 统一渲染，日志过滤改为 useMemo 优化性能。
- 构建日志面板使用 LogConsole 统一渲染，部署日志按任务 ID 精确订阅，减少无关状态更新。
- 全局 CSS 硬编码颜色迁移为 CSS 变量引用，提升视觉一致性。
- 部署、产物、服务页面自动隐藏侧边栏，主工作区获得更大展示空间。
- 顶部项目标签支持点击弹出项目切换弹窗，快捷切换项目更便捷。
- 移除 MSI 安装包构建目标，仅保留 NSIS 安装包，简化发布流程。

### 修复

- 修复普通账号部署到需 root 权限目录时上传和替换步骤失败的问题。
- 修复启动探针在非 root 部署场景下无法读取 PID 文件、日志文件和检测端口的问题。
- 修复服务映射在项目重新解析后模块 ID 失效导致关联断裂的问题。
- 修复 SFTP 上传路径含空格时 mkdir 命令执行失败的问题。

## [2.0.1] - 2026-04-29

### 新增

- 项目升级为打包部署工作台

### 优化

- 部署中心服务器列表从 List 布局重构为 Table 布局，支持分页浏览，信息展示更紧凑清晰。
- 部署中心服务器管理新增搜索功能，支持按名称、地址、分组关键词快速筛选服务器。
- 服务页面服务器列表新增搜索功能，支持按名称、地址关键词筛选，并增加分页控制。
- 部署、产物、服务三个页面自动隐藏侧边栏，主工作区获得更大展示空间。
- 支持快捷切换项目

### ⚠️ 注意

- 本次更新服务映射数据结构存在不兼容变更，历史服务映射配置可能无法正常加载，升级前请备份 `app.sqlite3` 数据库文件，慎重更新。

### 移除

- 移除部署、产物、服务页面的侧边栏冗余信息面板（部署上下文、产物筛选、服务器资源与最近部署），减少信息噪音。

## [2.0.0] - 2026-04-29

### 新增

- 新增 Maven 模块依赖图分析，支持查看模块依赖、被依赖、父子聚合关系、循环依赖提示与推荐联动模块。
- 新增"部署中心"，包含服务器管理、服务映射配置和一键部署入口。
- 新增服务器密码安全存储，密码在本地持久化前使用 Windows DPAPI 加密，不再以明文形式散落在配置中。
- 新增本地部署闭环流程，支持基于构建产物执行上传、停止旧服务、替换文件、启动服务、健康检查并记录部署历史。
- 新增可编排部署流水线，支持 6 种步骤类型：SSH 命令、等待、端口检测、HTTP 健康检查、日志关键字检测、文件上传，每个步骤可独立配置超时、重试次数和失败策略（停止/继续/回滚）。
- 新增 3 套内置部署模板：Spring Boot Jar 滚动替换、Tomcat War 替换、静态站点压缩包发布，支持基于模板快速创建服务映射。
- 新增启动探针检测，部署完成后自动验证服务是否真正启动，支持进程探针（PID 文件）、端口探针、HTTP 探针和日志探针四维检测，可配置超时时间、检测间隔和成功策略。
- 新增部署备份与回滚机制，部署前自动备份旧版本，支持配置备份保留数量、备份目录、失败自动回滚和回滚后重启旧版本。
- 新增部署文件上传进度实时展示，SFTP 优先传输，SFTP 不可用时自动降级为 Base64 分块上传，上传速度和进度实时反馈。
- 新增 SSH 原生连接库替代系统 ssh/scp/plink/pscp，密码认证和私钥认证均通过 Rust SSH 库直连，不再依赖本机安装 SSH 客户端。
- 新增部署停止功能，支持随时中断正在执行的部署任务，当前步骤完成后立即终止后续步骤。
- 新增"产物"页面，集中展示当前和历史构建产物，支持定位文件和删除。
- 新增"服务"页面，展示所有服务映射的部署状态、启动探针结果和远端进程信息。
- 新增右侧检查器抽屉（Inspector Drawer），统一承载构建日志、部署日志、诊断结果和部署详情，支持构建/部署日志源切换。
- 新增底部操作栏（Bottom ActionBar），构建状态、目标模块、命令编辑和一键构建/停止操作始终可见。
- 新增构建后续操作面板，构建成功后可直接选择服务映射和服务器发起部署，实现"构建→部署"一键衔接。
- 新增部署拓扑服务，根据模块路径和产物匹配规则自动关联构建产物与服务映射。
- 新增导航状态管理，主界面改为"构建/产物/部署/服务/环境/历史"六页签 ActivityBar 导航，运行中的构建和部署显示动态徽标。
- 新增上传进度和部署日志独立状态管理，部署过程实时更新不干扰其他交互。

### 优化

- 主工作台升级为 ActivityBar + Sidebar + MainWorkspace + Inspector 四区布局，功能导航更清晰。
- 部署中心面板重构为服务器管理、服务映射配置、部署执行和部署历史四合一视图，配置与执行一体化。
- 部署历史表增强，展示部署阶段流水线进度、每步耗时、探针检测结果和回滚状态。
- 停止旧服务逻辑增强，自动识别端口占用并精准查杀 Java 进程，避免误杀非 Java 服务；支持 lsof/ss/fuser/proc 多种端口探测方式兼容不同 Linux 发行版。
- 部署命令模板支持丰富的变量替换（${artifactPath}、${remoteDeployPath}、${pidFile}、${logFile}、${portProbePort} 等），减少硬编码。
- 旧版自定义命令自动迁移为流水线步骤格式，兼容历史部署配置。

### 移除

- 移除任务流水线（Task Pipeline）功能及相关代码（TaskPipelinePanel、TaskPipelineHistoryTable、task_pipeline_executor、pipeline_repo），由部署流水线统一替代。
- 移除独立模板面板（TemplatePanel），构建模板功能合并到构建配置区。
- 移除对系统 ssh/scp/plink/pscp 的依赖，部署传输改为 Rust SSH 库直连。

### 说明

- 当前远端部署默认按 Linux/类 Unix 目录与命令习惯执行 `mkdir/mv` 等操作，适合开发/测试环境闭环。
- SFTP 传输优先，若服务器不支持 SFTP 子系统会自动降级为 Base64 分块上传，无需额外配置。

## [1.3.2] - 2026-04-23

### 新增

- 构建进程使用 Windows Job Object 管理，停止构建时可通过 Job Object 终止整个进程树，确保 Maven 及其子进程（如 javac、surefire 等）被彻底清理。
- 取消构建新增"停止中"过渡状态，停止按钮在停止请求处理期间自动禁用，避免重复点击。
- 取消构建改为异步执行，停止操作在后台线程完成进程终止与状态确认，不再阻塞主界面。
- 取消构建全流程日志反馈，从发送停止请求到进程退出，每一步操作均在日志中输出系统提示。
- 模块树加载时展示 Spin 加载动画和"正在解析项目模块..."提示，替代空白等待。

### 优化

- 构建摘要区域从 Card 布局重构为 Dock 布局，状态标签、目标模块和操作按钮在同一行紧凑排列，减少纵向空间占用。
- 构建产物列表改为紧凑模式，最大高度 120px 内滚动展示，不再撑开构建摘要区域。
- 命令预览区域移至构建配置 Tabs 下方，操作命令与配置参数更紧密关联。
- 停止构建采用 Job Object 终止 + PowerShell 进程树清理 + taskkill 三重策略，提升进程清理可靠性。
- 切换项目时自动重置模块选择、构建状态和诊断结果，避免残留数据干扰新项目。

### 修复

- 修复停止构建后 Maven 子进程（如 javac、surefire）可能继续运行未被终止的问题。
- 修复取消构建时界面短暂无响应的问题，停止操作现在在后台线程执行。
- 修复构建进程启动与停止请求并发时可能导致状态不一致的问题。

## [1.3.1] - 2026-04-23

### 新增

- 环境中心新增"环境方案"能力，支持保存、切换和删除多套 JDK、Maven、settings.xml、本地仓库与 Maven Wrapper 配置。
- 支持"自动识别 / 指定环境方案"两种环境模式，切换方案后会自动刷新环境检测结果并重新生成构建命令。

### 优化

- 重构环境设置数据结构，将环境覆盖项统一收敛到环境方案中，减少配置状态分散带来的维护成本。
- 优化环境中心窄栏布局，方案选择、方案保存、路径覆盖和执行器切换在中间栏宽度下更紧凑稳定。
- 手动覆盖路径区改为标签化布局，路径输入框自适应宽度，操作按钮统一为图标按钮，减少横向挤压。

### 修复

- 修复本地仓库路径没有正确读取 `settings.xml` 中 `<localRepository>` 的问题。
- 修复 `<localRepository>` 使用命名空间、换行、注释或 `${user.home}`、`${env.xxx}` 等变量时无法识别的问题。
- 修复环境中心路径过长导致横向滚动、按钮被挤出可视区域的问题。

## [1.3.0] - 2026-04-22

### 新增

- 将原"环境检测"升级为"环境中心"，统一展示 JDK、Maven、Maven Wrapper、settings.xml、本地仓库和 Git 状态。
- 环境中心支持一键刷新检测，并展示环境来源，包括自动识别、手动覆盖、Wrapper 和未识别状态。
- 支持手动覆盖 JDK、Maven、settings.xml 和本地仓库路径，覆盖后会立即刷新环境并重新生成构建命令。
- 手动指定 `settings.xml` 和本地仓库后，构建命令会自动追加 `-s` 与 `-Dmaven.repo.local` 参数。
- 构建失败后自动生成规则诊断结果，支持识别 JDK 版本不匹配、Maven 缺失、Wrapper 失效、settings.xml 缺失、依赖下载失败、私服不可达、profile 不存在、模块路径错误、单元测试失败和未知错误。
- 右侧日志区域新增诊断面板，展示错误摘要、可能原因、建议动作和高价值关键日志行，并支持一键复制诊断结果。

### 优化

- 环境中心卡片改为更稳定的单列排布，避免路径、版本号和状态标签在中间栏宽度下挤压换行。
- 执行器切换从长条开关改为 `Maven / mvnw` 分段按钮，Wrapper 不存在时仅禁用 `mvnw` 选项。
- 手动覆盖路径输入区改为自适应布局，避免按钮文本较长时挤压输入框。
- 日志解析和诊断规则拆分为独立前端服务，为后续接入 AI 分析预留扩展点。

### 修复

- 修复 React 开发模式下构建日志事件监听可能重复注册，导致实时日志每行显示两次的问题。
- 修复构建日志接收到相邻重复事件时没有去重的问题。
- 修复环境中心部分状态标签和路径文本显示不全、换行错乱的问题。

## [1.2.1] - 2026-04-22

### 优化

- Git 检查远端、拉取更新、切换分支和读取提交列表改为后台执行，避免远端网络响应慢时拖动窗口导致应用无响应。
- 环境检测、项目解析、构建产物扫描以及历史记录、模板、环境设置等本地读写任务改为后台执行，提升主界面响应稳定性。
- 取消构建时不再等待 `taskkill` 输出，减少停止操作造成的短暂卡顿。
- 补充并启用 Rust 格式化工具链，统一 Rust 代码格式。

### 修复

- 修复 Git 检查远端或拉取失败时，错误信息显示到项目选择区域并且不易消失的问题；现在 Git 错误仅在 Git 状态卡片内展示，并支持手动关闭。

## [1.2.0] - 2026-04-22

### 新增

- 支持多项目管理，可保存多个 Maven 项目，并在项目面板中通过滚动列表快速切换。
- 左侧工作区改为 `项目 / Git / 模块 / 常用` 导航模式，点击后展开对应功能面板，提升左栏空间利用率。
- 常用组合支持保存、应用、编辑名称、置顶、取消置顶和删除。
- 历史记录支持一键重跑、恢复构建参数、复制命令和快速打开目录。
- 构建成功后自动扫描 `jar/war` 产物，展示文件名、大小、模块路径，并支持直接在资源管理器中定位。
- 构建结束后支持桌面通知和提示音反馈。
- Git 面板新增最近提交列表，展示提交标题、短 hash、作者和提交时间，并支持手动刷新。

### 优化

- 左侧导航栏加宽，模块树、项目列表、常用组合和 Git 信息展示更舒展。
- 项目选择从下拉框改为滚动列表，当前项目高亮显示，项目切换更直观。
- 单模块 Maven 项目会在模块列表中展示为根模块，不再出现模块列表为空的问题。
- Git 提交列表会在切换项目、检查远端、拉取更新和切换分支后自动刷新。
- 打开构建产物文件时会直接定位到文件，而不是只打开父目录。

### 修复

- 修复单模块项目没有 `<modules>` 时模块树不显示根项目的问题。
- 修复左侧项目、Git 和常用组合面板内容不足时出现大面积空白、信息密度偏低的问题。

## [1.1.1] - 2026-04-22

### 优化

- 常用组合列表简化为仅展示组合名称和应用按钮，减少左侧信息噪音。
- 常用组合卡片固定高度并支持内部滚动，避免组合数量增加后撑开布局。
- 中间构建配置区固定 Tab 栏，仅内容区域滚动，提升参数配置时的稳定性。
- Git 本地未提交改动提示改为紧凑文案，减少左侧 Git 状态卡片占用空间。
- 日志、历史、模板的放大查看入口统一改为全屏图标按钮，并为模板列表补充放大查看能力。
- 移除顶部重复版本展示，仅保留检查更新组件中的当前版本信息。

### 修复

- 修复常用组合卡片底部边缘与主工作区底部不齐的问题。
- 修复常用组合模块信息过长导致列表撑宽的问题。
- 修复顶部版本号曾写死为旧版本的问题。

## [1.1.0] - 2026-04-22

### 新增

- 主界面重构为专业级三栏工作台：顶部上下文 Header、左侧项目与模块区、中间构建配置区、右侧执行输出区。
- 中间配置区新增 `执行配置 / 环境检测 / 高级参数` Tabs，并新增高级参数面板，支持本地仓库、revision、并行线程和追加 Maven 参数。
- 右侧输出区新增 `日志 / 历史 / 模板` Tabs，日志区支持搜索、复制、清空、停止、放大和自动滚动控制。
- 左侧新增独立 Git 状态卡片和常用组合卡片，常用组合支持保存当前配置并一键应用。

### 优化

- 构建摘要卡片突出显示当前状态、目标模块、耗时和主操作按钮。
- 模块树支持短名称显示、完整 artifactId 悬浮提示、清空选择、展开全部、收起全部和仅显示已选模块。
- 常用组合卡片固定高度并支持内部滚动，避免模板数量增加后撑开左侧布局。
- 中间配置区 Tab 栏固定显示，仅内容区域滚动，减少配置时的上下查找成本。
- Git 本地未提交改动提示改为紧凑文案，减少左侧空间占用。
- 统一卡片圆角、边框、间距和浅色视觉风格，提升桌面工具完成度。

### 修复

- 修复左侧常用组合卡片底部边缘与主工作区底部不齐的问题。
- 修复常用组合模块信息过长导致列表撑宽的问题，现仅展示组合名称和应用按钮。

## [1.0.4] - 2026-04-22

### 新增

- 选择 Git 项目后展示当前分支和本地 Git 状态，避免自动联网触发 Git 凭据弹窗。
- 支持手动执行 `git fetch` 检查远端未拉取提交。
- 支持在应用内执行 `git pull --ff-only`，只允许干净更新，不允许自动合并,并提示用户优先在代码编辑器中拉取以便处理冲突。
- 支持展示当前 Git 分支，并可在本地分支之间切换。
- 应用内 Git 命令默认使用 Git Credential Manager，减少 CredentialHelperSelector 弹窗。
- 优化主界面为局部滚动布局，并压缩环境识别模块的信息展示。
- 将命令预览移动到工作区首位，提升高频打包操作的可见性。


## [1.0.3] - 2026-04-22

### 新增

- 优化更新提示展示


## [1.0.2] - 2026-04-22

### 新增

- 使用 SQLite 保存构建历史、常用模板和环境设置。

### 修复

- 修复某个问题。


## [1.0.0] - 2026-04-22

### 新增

- 集成 GitHub Releases 自动更新能力。
- 支持从应用内检查、下载并安装新版本。
- 使用 SQLite 保存构建历史、常用模板和环境设置。

### 优化

- 发布流程会生成并校验 `latest.json`，确保客户端能拉取更新信息。
